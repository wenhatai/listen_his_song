<?php
	require_once("../modules/class.song.php");
	$sid=$_POST['id'];
	$myopenid=$_POST['openid'];
	$song=new Song();
	$i=$song->is_myplaylist($sid,$myopenid);
	if($i){
		echo $i;
		return ;
	}
	$result=$song->song_info($sid);
	$song->add_myplaylist($myopenid,$sid);
	if($result!=""){
		echo(json_encode($result));
	}
?>