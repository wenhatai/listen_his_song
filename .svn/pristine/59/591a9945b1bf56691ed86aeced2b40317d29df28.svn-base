<?php
	require_once("../modules/class.search.php");
	$name = $_REQUEST['name'];
	$idarray = $_REQUEST['idarray'];
	$k=$_REQUEST['k'];
	$string=explode("|",$idarray);
	$array = array();
	for($i=0;$i<$k;$i++)
    {
	  $array[$i]=$string[$i+1];  //把点击了的歌曲的id收集起来，并每个放在数组array中
	  echo $array[$i];
    }
	$search = new Search();
	for($i=0;$i<$k;$i++)
	{
	   $search->songchangekey($array[$i],$name);
	}
?>
	
	