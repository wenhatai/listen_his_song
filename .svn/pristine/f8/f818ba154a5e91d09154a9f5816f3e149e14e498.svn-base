<?php
	require_once("../modules/class.song.php");
	session_start();
	if(isset($_SESSION['uid'])){
		$uid=$_SESSION['uid'];
		$del=$_POST['del'];
		$song=new Song();
		if($song->delgenre($uid,$del)){
			echo "success";
		}
		else{
			echo "fail";
		}
	}
?>